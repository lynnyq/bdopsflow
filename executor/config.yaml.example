# Executor 执行器配置
app:
  executor_name: "executor-1"
  hostname: "localhost"
  capacity: 10

# Scheduler gRPC 地址
scheduler:
  addr: "localhost:50051"
  timeout: 30

# 日志配置
log:
  level: "info"
  format: "json"
